import 'package:flutter/material.dart';

class ThemeStyle {
  ThemeData Light = ThemeData.light().copyWith(primaryColor: Colors.blue);

  ThemeData Dark = ThemeData.dark().copyWith(primaryColor: Colors.blueAccent);
}
